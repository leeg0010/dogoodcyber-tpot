- name: mysql
  config:
    databases:
      information_schema:
        path: ":memory:"
      mysql:
        path: ":memory:"
      threat_intel_db:
        path: ":memory:"
      captured_creds:
        path: ":memory:"
      recon_data:
        path: ":memory:"
      webapp_prod:
        path: ":memory:"
      backups:
        path: ":memory:"
      malware_samples:
        path: ":memory:"
      honeypot_logs:
        path: ":memory:"
